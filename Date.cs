namespace dotnet6
{
    class Thursday
    {
        public static bool Checkdate()
        {
            DateTime Today = new DateTime();
            Console.WriteLine(Today.DayOfWeek);
            return false;
        }
    }
}
